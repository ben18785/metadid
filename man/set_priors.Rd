% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{set_priors}
\alias{set_priors}
\title{Set prior distributions for the meta-analysis model}
\usage{
set_priors(
  treatment_effect_mean = normal(0, 10),
  treatment_effect_sd = cauchy(5),
  time_trend_mean = normal(0, 10),
  time_trend_sd = cauchy(5),
  rho_mean = normal(0, 1),
  rho_sd = normal(0, 0.5),
  nu = gamma(2, 0.1),
  delta_rct = normal(0, 10),
  delta_pp = normal(0, 10),
  sigma = cauchy(5),
  beta_cov = normal(0, 10),
  lkj_eta = lkj(2),
  baseline_difference_mean = normal(0, 0.5),
  baseline_difference_sd = cauchy(0.1),
  baseline_per_study = NULL,
  multiplier = lognormal(0, 0.7)
)
}
\arguments{
\item{treatment_effect_mean}{Prior on the population treatment effect mean.
Default: \code{normal(0, 10)}.}

\item{treatment_effect_sd}{Prior on the between-study SD. Default: \code{cauchy(5)}.}

\item{time_trend_mean}{Prior on the population time-trend mean. Default: \code{normal(0, 10)}.}

\item{time_trend_sd}{Prior on the between-study time-trend SD. Default: \code{cauchy(5)}.}

\item{rho_mean}{Prior on the mean of the Fisher-z transformed pre-post
correlation (only used when \code{hierarchical_rho = TRUE}). Default: \code{normal(0, 1)}.}

\item{rho_sd}{Prior on the SD of the Fisher-z transformed pre-post correlation
(only used when \code{hierarchical_rho = TRUE}). Default: \code{normal(0, 0.5)}.}

\item{nu}{Prior on the degrees of freedom for between-study heterogeneity
(only used when \code{robust_heterogeneity = TRUE}). Default: \code{gamma(2, 0.1)}.}

\item{delta_rct}{Prior on the RCT design offset relative to DiD
(only used when \code{design_effects = TRUE}). Default: \code{normal(0, 10)}.}

\item{delta_pp}{Prior on the Pre-Post design offset relative to DiD
(only used when \code{design_effects = TRUE}). Default: \code{normal(0, 10)}.}

\item{sigma}{Prior on the study-level observation standard deviations
(shared across all designs). Default: \code{cauchy(5)}.}

\item{beta_cov}{Prior on the covariate regression coefficients
(only used when \code{covariates} is specified in \code{\link[=meta_did]{meta_did()}}).
Default: \code{normal(0, 10)}.}

\item{lkj_eta}{Prior on the Cholesky factor of the correlation matrix
between treatment effects and time trends (only used when
\code{correlated_effects = TRUE}). Default: \code{lkj(2)}, which gently
regularises toward zero correlation.}

\item{baseline_difference_mean}{Prior on the population mean of the
per-study baseline imbalance \eqn{\delta_i = (b_{T,i} - b_{C,i}) / b_{C,i}},
defined as the fractional difference between the treatment-arm and
control-arm pre-treatment baselines, expressed as a fraction of the
control-arm baseline (the control-pre reference convention).
Constrained to \eqn{\delta_i > -1} so that the derived
\eqn{(1 + \delta_i)} factor remains positive in both modelled-mode
parameterisations. Only used when \code{baseline_imbalance = "estimated"}.
Default: \code{normal(0, 0.5)}.}

\item{baseline_difference_sd}{Prior on the between-study SD of the
baseline imbalance. Only used when \code{baseline_imbalance = "estimated"}.
Default: \code{cauchy(0.1)}.}

\item{multiplier}{Prior on the multiplicative-covariate effect multiplier
(only used when \code{multiplicative_covariate} is specified in \code{\link[=meta_did]{meta_did()}}).
With one or two multiplicative covariates the same prior is applied
independently to every estimated non-reference-level factor (of either
covariate). Must be a \code{\link[=lognormal]{lognormal()}} prior,
placed on the log of the multiplier so it is strictly positive with no
boundary at zero. Default: \code{lognormal(0, 0.7)} — a median of 1 (the
no-multiplicative-effect case), with a central 95\% range of roughly
\verb{[0.25, 3.9]} on the natural scale.}
}
\value{
A \code{did_priors} object.
}
\description{
Construct a \code{did_priors} object specifying the prior distribution for each
population-level parameter. Any parameter not supplied takes a default.
}
\examples{
# Use defaults
set_priors()

# Override one prior
set_priors(treatment_effect_sd = cauchy(2))
}
