% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_individual_data}
\alias{validate_individual_data}
\title{Validate individual-level study data}
\usage{
validate_individual_data(data)
}
\arguments{
\item{data}{A data frame in long format with one row per observation.
Required columns: \code{study_id}, \code{design}, \code{group}, \code{time}, \code{value}.
For repeated-measures designs (\code{"did"} and \code{"pp"}), a \code{subject_id}
column is also required to correctly pair pre and post observations.
Valid designs: \code{"did"}, \code{"rct"}, \code{"pp"}.}
}
\value{
\code{data} invisibly if valid; otherwise stops with an informative error.
}
\description{
Checks that \code{data} meets the structural requirements for use in
\code{\link[=meta_did]{meta_did()}}. Called automatically inside \code{\link[=meta_did]{meta_did()}}; also useful for
diagnosing problems before fitting.
}
