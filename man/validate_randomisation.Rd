% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_randomisation}
\alias{validate_randomisation}
\title{Validate the assignment-mechanism columns}
\usage{
validate_randomisation(summary_data, individual_data)
}
\arguments{
\item{summary_data}{Summary-level data frame (or NULL).}

\item{individual_data}{Individual-level data frame (or NULL).}
}
\value{
Invisible NULL. Stops with an error if validation fails.
}
\description{
Checks the optional \code{randomisation} column (and its \code{cluster_size} / \code{icc}
companions) in either data source. Randomisation is deliberately \strong{never}
inferred from \code{design}: a post-only study may be a randomised trial or an
unrandomised matched cohort, and a DiD study may be a cluster-randomised
roll-out. Treating \code{design == "rct"} as a randomisation claim is exactly the
error this column exists to prevent, so an absent or \code{NA} value is read as
\code{"none"} (imbalance estimated) rather than as randomisation.
}
\keyword{internal}
