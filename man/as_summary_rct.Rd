% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{as_summary_rct}
\alias{as_summary_rct}
\title{Summarise simulated DiD data as RCT-style post-only statistics}
\usage{
as_summary_rct(sim)
}
\arguments{
\item{sim}{A data frame produced by \code{\link[=simulate_meta_did]{simulate_meta_did()}}.}
}
\value{
A data frame with one row per study in the format required by
\code{\link[=validate_summary_data]{validate_summary_data()}} for design \code{"rct"}.
}
\description{
Discards all pre-treatment observations and returns post-treatment means and
SDs for both arms. Produces data in the format expected by \code{\link[=meta_did]{meta_did()}} for
design \code{"rct"}.
}
\examples{
sim <- simulate_meta_did(n_studies = 3, seed = 1)
as_summary_rct(sim)
}
