% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_summary_data}
\alias{validate_summary_data}
\title{Validate summary-level study data}
\usage{
validate_summary_data(data)
}
\arguments{
\item{data}{A data frame with one row per study. Must contain \code{study_id}
and \code{design} columns. Valid designs:
\itemize{
\item \code{"did"}: DiD studies with separate pre/post summaries for both groups.
\item \code{"did_change"}: DiD studies reporting only change means and SDs.
\item \code{"rct"}: RCT studies with post-treatment summaries only.
\item \code{"pp"}: Pre-post studies with no control group.
}

The \code{rho} column (pre-post correlation) is optional for \code{"did"} and
\code{"pp"} designs; missing values trigger hierarchical imputation when
\code{hierarchical_rho = TRUE}.}
}
\value{
\code{data} invisibly if valid; otherwise stops with an informative error.
}
\description{
Checks that \code{data} meets the structural requirements for use in
\code{\link[=meta_did]{meta_did()}}. Called automatically inside \code{\link[=meta_did]{meta_did()}}; also useful for
diagnosing problems before fitting.
}
