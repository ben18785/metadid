% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{as_summary_pp}
\alias{as_summary_pp}
\title{Summarise simulated DiD data as pre-post (treatment arm only) statistics}
\usage{
as_summary_pp(sim)
}
\arguments{
\item{sim}{A data frame produced by \code{\link[=simulate_meta_did]{simulate_meta_did()}}.}
}
\value{
A data frame with one row per study in the format required by
\code{\link[=validate_summary_data]{validate_summary_data()}} for design \code{"pp"}.
}
\description{
Discards all control-arm observations and returns pre/post means and SDs for
the treatment arm. Produces data in the format expected by \code{\link[=meta_did]{meta_did()}} for
design \code{"pp"}.
}
\examples{
sim <- simulate_meta_did(n_studies = 3, seed = 1)
as_summary_pp(sim)
}
