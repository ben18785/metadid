% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_meta_did}
\alias{simulate_meta_did}
\title{Simulate individual-level DiD data for a collection of studies}
\usage{
simulate_meta_did(
  n_studies = 20L,
  n_control = 100L,
  n_treatment = 100L,
  true_effect = -0.15,
  sigma_effect = 0.03,
  true_trend = -0.02,
  sigma_trend = 0,
  baseline_mean = 0.45,
  baseline_sd = 0,
  within_sd = 0.12,
  rho = 0.5,
  seed = NULL
)
}
\arguments{
\item{n_studies}{Number of studies. Default \code{20}.}

\item{n_control}{Number of individuals per study in the control arm.
Default \code{100L}.}

\item{n_treatment}{Number of individuals per study in the treatment arm.
Default \code{100L}.}

\item{true_effect}{Population mean treatment effect. Default \code{-0.15}.}

\item{sigma_effect}{Between-study SD of treatment effects. Default \code{0.03}.}

\item{true_trend}{Population mean time trend. Default \code{-0.02}.}

\item{sigma_trend}{Between-study SD of time trends. Default \code{0}.}

\item{baseline_mean}{Population mean of study baselines. Default \code{0.45}.}

\item{baseline_sd}{Between-study SD of baselines. Default \code{0}.}

\item{within_sd}{Individual-level SD (the same for pre and post and for
both groups). Default \code{0.12}.}

\item{rho}{Pre-post correlation within individuals. Directly parameterises
the off-diagonal of the bivariate normal covariance matrix. Default \code{0.5}.}

\item{seed}{Integer random seed for reproducibility. Default \code{NULL}.}
}
\value{
A data frame with columns \code{study_id}, \code{subject_id}, \code{group}
(\code{"control"} or \code{"treatment"}), \code{time} (\code{"pre"} or \code{"post"}), \code{value}.
The true study-level parameters are attached as attribute \code{"true_params"}.
}
\description{
Generates individual-level pre/post observations for both arms of a set of
difference-in-differences studies under a hierarchical model. This is the
gold-standard view of the data; the \verb{as_*} family of functions then produce
the data as it would appear under different levels of access.
}
\section{Data-generating model}{

Study-level parameters are drawn hierarchically:
\deqn{\theta_i \sim \text{Normal}(\texttt{true\_effect},\, \texttt{sigma\_effect}^2)}
\deqn{\gamma_i \sim \text{Normal}(\texttt{true\_trend},\, \texttt{sigma\_trend}^2)}
\deqn{b_i      \sim \text{Normal}(\texttt{baseline\_mean},\, \texttt{baseline\_sd}^2)}

Within each study, individual (pre, post) pairs are drawn from a bivariate
normal distribution with covariance matrix
\deqn{\Sigma = \sigma^2 \begin{pmatrix} 1 & \rho \\ \rho & 1 \end{pmatrix}}
making the role of \eqn{\rho} as the pre-post correlation explicit. The mean
vector for the control group is \eqn{(b_i,\, b_i + \gamma_i)} and for the
treatment group \eqn{(b_i,\, b_i + \gamma_i + \theta_i)}.
}

\examples{
dat <- simulate_meta_did(n_studies = 10, true_effect = -0.15, seed = 42)
head(dat)
attr(dat, "true_params")
}
\seealso{
\code{\link[=as_individual_did]{as_individual_did()}}, \code{\link[=as_individual_rct]{as_individual_rct()}}, \code{\link[=as_individual_pp]{as_individual_pp()}},
\code{\link[=as_summary_did]{as_summary_did()}}, \code{\link[=as_summary_rct]{as_summary_rct()}}, \code{\link[=as_summary_pp]{as_summary_pp()}},
\code{\link[=as_summary_did_change]{as_summary_did_change()}}
}
