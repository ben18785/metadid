# Specify a gamma prior

Used for the degrees-of-freedom parameter when
`robust_heterogeneity = TRUE`.

## Usage

``` r
gamma(shape, rate)
```

## Arguments

- shape:

  Shape parameter (must be positive).

- rate:

  Rate parameter (must be positive).

## Value

A `did_prior` object.
