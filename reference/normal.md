# Specify a normal prior

Specify a normal prior

## Usage

``` r
normal(mean, sd)
```

## Arguments

- mean:

  Prior mean.

- sd:

  Prior standard deviation (must be positive).

## Value

A `did_prior` object.
